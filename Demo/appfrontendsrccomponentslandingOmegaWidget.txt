
/app/frontend/src/components/landing/OmegaWidget.jsx


I noticed the bars in the widget aren't rendering (no explicit height, so scaleY has nothing to scale). Let me fix that:
Action: file_editor str_replace /app/frontend/src/components/landing/OmegaWidget.jsx --old-str "          <div className=\"flex items-end justify-between h-40 gap-3 md:gap-4\">
            {BAR_DATA.map((b, i) => {
              const visible = bars.includes(i);
              return (
                <div key={b.label} className=\"flex-1 flex flex-col items-center gap-2\">
                  <div className=\"relative w-full h-32 flex items-end\">
                    <motion.div
                      initial={{ scaleY: 0 }}
                      animate={{ scaleY: visible ? b.h / 100 : 0 }}
                      transition={{
                        duration: 0.7,
                        ease: [0.19, 1, 0.22, 1],
                      }}
                      style={{
                        background: b.color,
                        transformOrigin: \"bottom\",
                      }}
                      className=\"w-full rounded-t-md relative\"
                    >
                      {b.color === \"#FF5E00\" && visible && (
                        <span className=\"absolute -top-6 left-1/2 -translate-x-1/2 font-mono text-[10px] font-semibold text-[#FF5E00] whitespace-nowrap\">
                          peak
                        </span>
                      )}
                    </motion.div>
                  </div>" --new-str "          <div className=\"flex items-end justify-between h-40 gap-3 md:gap-4\">
            {BAR_DATA.map((b, i) => {
              const visible = bars.includes(i);
              return (
                <div key={b.label} className=\"flex-1 flex flex-col items-center gap-2\">
                  <div className=\"relative w-full h-32 flex items-end\">
                    <motion.div
                      initial={{ height: 0, opacity: 0 }}
                      animate={{
                        height: visible ? `${b.h}%` : 0,
                        opacity: visible ? 1 : 0,
                      }}
                      transition={{
                        duration: 0.7,
                        ease: [0.19, 1, 0.22, 1],
                      }}
                      style={{ background: b.color }}
                      className=\"w-full rounded-t-md relative\"
                    >
                      {b.color === \"#FF5E00\" && visible && (
                        <span className=\"absolute -top-6 left-1/2 -translate-x-1/2 font-mono text-[10px] font-semibold text-[#FF5E00] whitespace-nowrap\">
                          peak
                        </span>
                      )}
                    </motion.div>
                  </div>"
Observation: Edit was successful.